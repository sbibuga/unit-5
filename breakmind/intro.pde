void intro() {
  
  // variable setup
  
  
  // ui
  background(255);
  fill(0);
  textSize(150);
  textAlign(CENTER,CENTER);
  text("breaktuo",500,300);
  textSize(60);
  text("click to start",500,625);
}

void introClicksP() {
  mode = inProg;
}
